# 저장소 정리 기록 (2026-09-22)

이 커밋에서 다음을 정리했습니다.

1. 기존 이력에 있던 `source_code.zip`을 저장소 히스토리에서 완전히 제거했습니다(모든 커밋에서 삭제, force-push).
2. zip 안에 있던 실제 소스 코드(`ai/`, `back/`, `front/`)를 압축을 풀어 이 커밋에 추가했습니다.
3. 코드에 평문으로 들어 있던 DB 비밀번호, JWT 시크릿, 공공데이터 서비스 키, Kakao API 키, 메일 계정 정보를 환경변수 참조(`${VAR}`)로 교체했습니다. 실제 값은 `.env.example`에 이름만 남겼습니다.

이 커밋은 2026-09-22에 작성됐고, 과거에 실제로 이 구조로 개발됐던 것처럼 표기하지 않습니다. 원래 개발 당시의 커밋 이력(파일 업로드 방식)은 그대로 남아 있습니다.

**중요:** 이 정리는 이력에서 시크릿을 지운 것이지, 이미 노출됐던 값 자체를 무효화한 것은 아닙니다. 위 항목의 실제 값은 각 서비스(DB, JWT 발급처, 공공데이터포털, Kakao Developers, 메일 계정)에서 별도로 폐기·재발급해야 안전합니다.
