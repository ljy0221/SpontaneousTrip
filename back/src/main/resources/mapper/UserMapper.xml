<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.ssafy.passproject.domain.user.repository.UserRepository">

	<insert id="registUser" parameterType="com.ssafy.passproject.domain.user.entity.User" useGeneratedKeys="true" keyProperty="userId">
		INSERT INTO users (email, password, nickname, role)
		VALUES (#{email}, #{password}, #{nickname}, COALESCE(#{role}, 'ROLE_USER'))
	</insert>

	<select id="findByEmail" parameterType="string" resultType="com.ssafy.passproject.domain.user.entity.User">
		SELECT
		user_id   AS userId,
		email,
		password,
		nickname,
		role,
		join_date AS joinDate,
		last_login AS lastLogin
		FROM users
		WHERE email = #{email}
	</select>

	<update id="updateUser" parameterType="com.ssafy.passproject.domain.user.entity.User">
		UPDATE users
		SET
			nickname = #{nickname},
			password = #{password}
		WHERE user_id = #{userId}
	</update>

	<delete id="deleteUser" parameterType="int">
		DELETE FROM users
		WHERE user_id = #{userId}
	</delete>

	<update id="updateLastLogin" parameterType="string">
		UPDATE users
		SET last_login = NOW()
		WHERE email = #{email}
	</update>

</mapper>